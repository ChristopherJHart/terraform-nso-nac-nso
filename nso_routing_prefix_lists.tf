# GENERATED BY nac-nso-generator v0.1.0
# NED: cisco-ios-cli-6.115, cisco-nx-cli-5.37
# Mapping: mapping.yaml (sha256:da7dc6890f83...)
# Feature: routing.prefix_lists
#
# Code between BEGIN/END GENERATED markers is regenerated.
# Everything outside markers is manual and preserved across runs.


# ---- BEGIN GENERATED locals ----
locals {
  nso_routing_prefix_lists = flatten([
    for device in local.devices : [
      for item in try(local.device_config[device.name].prefix_lists, []) : {
        key         = format("%s/prefix_list/%s", device.name, item.name)
        device_name = device.name
        instance    = device.name
        os          = try(device.os, "iosxe")
        name  = item.name
        name             = try(item.name,
          null)
        description      = try(item.description,
          null)
      }
    ]
  ])
}
# ---- END GENERATED locals ----


# ---- BEGIN GENERATED resource prefix_list_nxos ----
resource "nso_device_config" "prefix_list_nxos" {
  for_each = { for v in local.nso_routing_prefix_lists : v.key => v if v.os == "nxos" }

  instance = each.value.instance
  device   = each.value.device_name
  path     = "tailf-ned-cisco-nx:ip/prefix-list/prefixes=${each.value.name}"
  delete   = true

  attributes = merge(
    { name = tostring(each.value.name) },
    each.value.name != null ? { "name" = each.value.name } : {},
    each.value.description != null ? { "description" = each.value.description } : {},
  )
}
# ---- END GENERATED resource prefix_list_nxos ----
# ---- BEGIN GENERATED resource prefix_list_iosxe ----
resource "nso_device_config" "prefix_list_iosxe" {
  for_each = { for v in local.nso_routing_prefix_lists : v.key => v if v.os == "iosxe" }

  instance = each.value.instance
  device   = each.value.device_name
  path     = "tailf-ned-cisco-ios:ip/prefix-list/prefixes=${each.value.name}"
  delete   = true

  attributes = merge(
    { name = tostring(each.value.name) },
    each.value.name != null ? { "name" = each.value.name } : {},
    each.value.description != null ? { "description" = each.value.description } : {},
  )
}
# ---- END GENERATED resource prefix_list_iosxe ----

