# GENERATED BY nac-nso-generator v0.1.0
# NED: cisco-ios-cli-6.115, cisco-nx-cli-5.37
# Mapping: mapping.yaml (sha256:da7dc6890f83...)
# Feature: aaa.radius_global
#
# Code between BEGIN/END GENERATED markers is regenerated.
# Everything outside markers is manual and preserved across runs.


# ---- BEGIN GENERATED locals ----
locals {
  nso_aaa_radius_global = flatten([
    for device in local.devices : [
      for item in try(local.device_config[device.name].aaa_radius_global, []) : {
        key         = format("%s/radius_global/%s", device.name, item.radius_global)
        device_name = device.name
        instance    = device.name
        os          = try(device.os, "iosxe")
        radius_global  = item.radius_global
        timeout          = try(item.timeout,
          null)
        retransmit       = try(item.retransmit,
          null)
        deadtime         = try(item.deadtime,
          null)
        directed_request = try(item.directed_request,
          null)
        key_type         = try(item.key_type,
          null)
        key_secret       = try(item.key_secret,
          null)
        key_encryption   = try(item.key_encryption,
          null)
        key_password     = try(item.key_password,
          null)
      }
    ]
  ])
}
# ---- END GENERATED locals ----


# ---- BEGIN GENERATED resource radius_global_nxos ----
resource "nso_device_config" "radius_global_nxos" {
  for_each = { for v in local.nso_aaa_radius_global : v.key => v if v.os == "nxos" }

  instance = each.value.instance
  device   = each.value.device_name
  path     = "tailf-ned-cisco-nx:radius-server"
  delete   = true

  attributes = merge(
    { radius_global = tostring(each.value.radius_global) },
    each.value.timeout != null ? { "timeout" = each.value.timeout } : {},
    each.value.retransmit != null ? { "retransmit" = each.value.retransmit } : {},
    each.value.deadtime != null ? { "deadtime" = each.value.deadtime } : {},
    each.value.directed_request == true ? { "directed-request" = "" } : {},
    each.value.key_encryption != null ? { "key/encryption" = each.value.key_encryption } : {},
    each.value.key_password != null ? { "key/password" = each.value.key_password } : {},
  )
}
# ---- END GENERATED resource radius_global_nxos ----
# ---- BEGIN GENERATED resource radius_global_iosxe ----
resource "nso_device_config" "radius_global_iosxe" {
  for_each = { for v in local.nso_aaa_radius_global : v.key => v if v.os == "iosxe" }

  instance = each.value.instance
  device   = each.value.device_name
  path     = "tailf-ned-cisco-ios:radius-server"
  delete   = true

  attributes = merge(
    { radius_global = tostring(each.value.radius_global) },
    each.value.timeout != null ? { "timeout" = each.value.timeout } : {},
    each.value.retransmit != null ? { "retransmit" = each.value.retransmit } : {},
    each.value.deadtime != null ? { "deadtime" = each.value.deadtime } : {},
    each.value.key_type != null ? { "key/type" = each.value.key_type } : {},
    each.value.key_secret != null ? { "key/secret" = each.value.key_secret } : {},
  )
}
# ---- END GENERATED resource radius_global_iosxe ----

