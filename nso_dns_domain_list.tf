# GENERATED BY nac-nso-generator v0.1.0
# NED: cisco-ios-cli-6.115, cisco-nx-cli-5.37
# Mapping: mapping.yaml (sha256:da7dc6890f83...)
# Feature: dns.domain_list
#
# Code between BEGIN/END GENERATED markers is regenerated.
# Everything outside markers is manual and preserved across runs.


# ---- BEGIN GENERATED locals ----
locals {
  nso_dns_domain_list = flatten([
    for device in local.devices : [
      for item in try(local.device_config[device.name].dns_domain_list, []) : {
        key         = format("%s/domain_list/%s", device.name, item.name)
        device_name = device.name
        instance    = device.name
        os          = try(device.os, "iosxe")
        name  = item.name
        name             = try(item.name,
          null)
      }
    ]
  ])
}
# ---- END GENERATED locals ----


# ---- BEGIN GENERATED resource domain_list_nxos ----
resource "nso_device_config" "domain_list_nxos" {
  for_each = { for v in local.nso_dns_domain_list : v.key => v if v.os == "nxos" }

  instance = each.value.instance
  device   = each.value.device_name
  path     = "tailf-ned-cisco-nx:ip/domain-list=${each.value.name}"
  delete   = true

  attributes = merge(
    { name = tostring(each.value.name) },
    each.value.name != null ? { "name" = each.value.name } : {},
  )
}
# ---- END GENERATED resource domain_list_nxos ----
# ---- BEGIN GENERATED resource domain_list_iosxe ----
resource "nso_device_config" "domain_list_iosxe" {
  for_each = { for v in local.nso_dns_domain_list : v.key => v if v.os == "iosxe" }

  instance = each.value.instance
  device   = each.value.device_name
  path     = "tailf-ned-cisco-ios:ip/domain/list=${each.value.name}"
  delete   = true

  attributes = merge(
    { name = tostring(each.value.name) },
    each.value.name != null ? { "name" = each.value.name } : {},
  )
}
# ---- END GENERATED resource domain_list_iosxe ----

