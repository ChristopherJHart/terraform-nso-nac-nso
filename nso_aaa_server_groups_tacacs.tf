# GENERATED BY nac-nso-generator v0.1.0
# NED: cisco-ios-cli-6.115, cisco-nx-cli-5.37
# Mapping: mapping.yaml (sha256:da7dc6890f83...)
# Feature: aaa.server_groups_tacacs
#
# Code between BEGIN/END GENERATED markers is regenerated.
# Everything outside markers is manual and preserved across runs.


# ---- BEGIN GENERATED locals ----
locals {
  nso_aaa_server_groups_tacacs = flatten([
    for device in local.devices : [
      for item in try(local.device_config[device.name].aaa_server_groups_tacacs, []) : {
        key         = format("%s/server_groups_tacac/%s", device.name, item.name)
        device_name = device.name
        instance    = device.name
        os          = try(device.os, "iosxe")
        name  = item.name
        name             = try(item.name,
          null)
        source_interface = try(item.source_interface,
          null)
        use_vrf          = try(item.use_vrf,
          null)
        deadtime         = try(item.deadtime,
          null)
        timeout          = try(item.timeout,
          null)
      }
    ]
  ])
}
# ---- END GENERATED locals ----


# ---- BEGIN GENERATED resource server_groups_tacac_nxos ----
resource "nso_device_config" "server_groups_tacac_nxos" {
  for_each = { for v in local.nso_aaa_server_groups_tacacs : v.key => v if v.os == "nxos" }

  instance = each.value.instance
  device   = each.value.device_name
  path     = "tailf-ned-cisco-nx:aaa/group/server/tacacs-plus=${each.value.name}"
  delete   = true

  attributes = merge(
    { id = tostring(each.value.name) },
    each.value.name != null ? { "id" = each.value.name } : {},
    each.value.source_interface != null ? { "source-interface" = each.value.source_interface } : {},
    each.value.use_vrf != null ? { "use-vrf" = each.value.use_vrf } : {},
    each.value.deadtime != null ? { "deadtime" = each.value.deadtime } : {},
  )
}
# ---- END GENERATED resource server_groups_tacac_nxos ----
# ---- BEGIN GENERATED resource server_groups_tacac_iosxe ----
resource "nso_device_config" "server_groups_tacac_iosxe" {
  for_each = { for v in local.nso_aaa_server_groups_tacacs : v.key => v if v.os == "iosxe" }

  instance = each.value.instance
  device   = each.value.device_name
  path     = "tailf-ned-cisco-ios:aaa/group/server/tacacs-plus=${each.value.name}"
  delete   = true

  attributes = merge(
    { name = tostring(each.value.name) },
    each.value.name != null ? { "name" = each.value.name } : {},
    each.value.timeout != null ? { "timeout" = each.value.timeout } : {},
  )
}
# ---- END GENERATED resource server_groups_tacac_iosxe ----

